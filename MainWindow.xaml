<Window x:Class="FinancyApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Financy Test" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="RUN ALL TESTS"
                    Click="RunTests_Click"
                    Height="40"
                    Width="180"
                    FontSize="16"
                    Background="#2e7d32"
                    Foreground="White"
                    Margin="0,0,10,0"/>

            <Button Content="RUN MY TESTS"
                    Click="RunMyTests_Click"
                    Height="40"
                    Width="180"
                    FontSize="16"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <TextBox x:Name="OutputBox"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="13"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>
        </ScrollViewer>
    </Grid>
</Window>